using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Result : MonoBehaviour {

    public Text questionTxt;
    public Text yourAnswerTxt;
    public Text trueAnswerTxt;
    public Text pointsTxt;
    //public Text totalTxt;


    ////EXAMPLE: DO NOT USE HERE
    //void Start()
    //{

    //    //this is what you would be doing in the main menu script:
    //    Result test = Instantiate(this, Vector3.zero, Quaternion.identity); //"this" would be the prefab

    //    test.transform.SetParent(this.transform);   //"this" is a reference to your scrollview's CONTENT object
    //}
}
